# 🚀 启动本地服务器

## 问题原因

当你直接双击打开 HTML 文件时，浏览器使用 `file://` 协议，这会导致：
- ❌ CORS 跨域请求被阻止
- ❌ 无法连接到 Supabase API
- ❌ 显示 "Failed to fetch" 错误

**解决方案**：使用本地服务器（`http://` 协议）

---

## 方法1：使用 Python（推荐）

### 检查是否安装了 Python

打开命令提示符（CMD），输入：
```bash
python --version
```

如果显示版本号（如 Python 3.x.x），说明已安装。

### 启动服务器

**方式A：双击批处理文件**
1. 双击 `启动本地服务器.bat` 文件
2. 等待服务器启动
3. 在浏览器中访问：`http://localhost:8000/index-supabase.html`

**方式B：手动启动**
1. 打开命令提示符（CMD）
2. 进入项目目录：
   ```bash
   cd C:\Users\Administrator\Desktop\code\2\password_manager
   ```
3. 启动服务器：
   ```bash
   python -m http.server 8000
   ```
4. 在浏览器中访问：`http://localhost:8000/index-supabase.html`

---

## 方法2：使用 Node.js

如果你安装了 Node.js：

### 安装 http-server

```bash
npm install -g http-server
```

### 启动服务器

```bash
cd C:\Users\Administrator\Desktop\code\2\password_manager
http-server -p 8000
```

然后访问：`http://localhost:8000/index-supabase.html`

---

## 方法3：使用 VS Code Live Server

如果你使用 VS Code：

1. 安装 "Live Server" 扩展
2. 右键点击 `index-supabase.html`
3. 选择 "Open with Live Server"

---

## 方法4：直接部署到 GitHub Pages（最简单）

这样就可以直接在线访问，不需要本地服务器：

```bash
# 提交代码
git add .
git commit -m "添加云同步版"
git push origin supabase-sync

# 然后在 GitHub 上启用 Pages
```

访问：`https://fanyilang.github.io/password_manager/index-supabase.html`

---

## 🎯 推荐方案

### 对于测试：使用 Python 本地服务器
- 快速
- 简单
- 不需要额外安装

### 对于实际使用：部署到 GitHub Pages
- 可以在任何设备访问
- 不需要启动服务器
- 自动 HTTPS 加密

---

## ❓ 常见问题

### Q: 没有安装 Python 怎么办？

**方案1：安装 Python**
1. 访问：https://www.python.org/downloads/
2. 下载并安装 Python
3. 安装时勾选 "Add Python to PATH"

**方案2：直接部署到 GitHub Pages**
- 不需要安装任何东西
- 直接在线使用

### Q: 端口 8000 被占用怎么办？

使用其他端口：
```bash
python -m http.server 8080
```

然后访问：`http://localhost:8080/index-supabase.html`

### Q: 关闭命令提示符后服务器会停止吗？

是的，所以：
- 测试时保持命令提示符窗口打开
- 或者直接部署到 GitHub Pages

---

## 🚀 现在就开始

### 最快的方法：

1. **双击 `启动本地服务器.bat`**
2. **等待显示 "Serving HTTP on..."**
3. **打开浏览器访问：`http://localhost:8000/index-supabase.html`**
4. **开始测试！**

---

**准备好了吗？** 双击 `启动本地服务器.bat` 文件，然后在浏览器中打开 `http://localhost:8000/index-supabase.html`！
